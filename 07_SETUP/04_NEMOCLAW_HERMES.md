# Step 4 — NemoClaw + Hermes

Use NVIDIA's maintained current quickstart, not a copied social-media command from an old release.

Preflight:
- Linux architecture/OS;
- Docker works for the installing user;
- >= 4 vCPU;
- >= 16 GB RAM preferred;
- >= 40 GB free disk;
- required Node/npm version;
- firewall/egress plan.

Create sandbox name:
`neewa-core`

After install:
- verify Hermes CLI/dashboard privately;
- verify sandbox network policy;
- create snapshot `baseline-hermes`;
- record exact NemoClaw/Hermes versions in `14_REFERENCE/DECISIONS_FREEZE.md`.

Never paste provider keys into Git or chat transcripts.
