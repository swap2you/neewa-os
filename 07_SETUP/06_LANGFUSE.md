# Step 6 — Langfuse

Start with Hobby/free.

Create project:
`NEEWA-OS`

Capture:
- project;
- job ID;
- agent/worker;
- model/provider;
- latency;
- input/output tokens;
- cost;
- success/failure;
- retry;
- evaluation score.

Do not send secrets or sensitive raw content to observability by default. Configure redaction.
