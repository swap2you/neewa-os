# Step 8 — OpenRouter

Purpose:
- model diversity;
- low-cost/open models;
- legitimate fallback;
- provider guardrails.

Configure:
- hard budget;
- Zero Data Retention where available/required;
- provider/model allowlist;
- prompt-injection/DLP guardrails where available;
- separate key/guardrail for low-priority experimental jobs.

Do not use account rotation or quota-circumvention techniques.
