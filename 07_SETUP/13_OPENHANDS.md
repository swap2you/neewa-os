# Step 13 — OpenHands Agent Canvas

Use Agent Canvas as the coding execution floor.

Connect existing:
- Codex subscription/login;
- Claude Code subscription/login;
- Gemini CLI login.

Rules:
- isolated worktree/branch per implementation job;
- one writer per file/write scope;
- independent reviewer;
- acceptance tests defined before implementation;
- no production merge/deploy without policy gate.

Record which subscription actually handled each job for later subscription optimization.
