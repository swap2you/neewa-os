# Configuration Examples

Files in this folder are examples only.

Never commit live secrets.

Production configuration should use:
- environment/secret manager;
- scoped keys;
- least privilege;
- separate dev/test/prod values;
- rotation records.
