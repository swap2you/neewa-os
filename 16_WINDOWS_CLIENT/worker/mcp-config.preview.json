{
  "note": "Preview only. Not merged into Cursor, Claude, or Codex. One local execution owner: neewa-windows-worker + the existing cua-driver daemon.",
  "cursor": {
    "mcpServers": {
      "cua-driver": {
        "command": "C:\\Users\\swap2\\AppData\\Local\\Programs\\Cua\\cua-driver\\bin\\cua-driver.exe",
        "args": ["mcp"],
        "type": "stdio"
      }
    }
  },
  "claude_cli": "INSTALLED — do not run: claude mcp add-json --scope user cua-computer-use ...",
  "codex_cli": "INSTALLED — do not run: codex mcp add cua-driver -- cua-driver.exe mcp",
  "gemini_cli": "INSTALLED — do not add cua-driver MCP",
  "antigravity_cli": "INSTALLED — do not add cua-driver MCP"
}
