# Implementation plan — autonomy v3

Locked order after G0:

1. Atomic JSON, collision-safe IDs, state graph includes BASELINE_LOCKED, runner lease.
2. Budget reservation + positive remaining cap.
3. Authorization: untrusted job JSON; denied repo; paraphrased A2/A3.
4. Workflow-specific requirements/design/council; spec_sha256.
5. Research synthesizer + citation validator + source pack.
6. Expand unit/NEG tests.
7. Update Hermes skill; ff-only core sync.
8. Live Conversation E2E-A and E2E-B; record evidence; do not count old jobs.

Fixture `demo-status` remains labeled regression-only.
