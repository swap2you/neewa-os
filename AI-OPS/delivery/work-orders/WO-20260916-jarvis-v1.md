# Work Order WO-20260916-jarvis-v1 (Cursor → NEEWA, read-only)

Cursor is the sole repo writer. NEEWA: perform READ-ONLY verification + independent review
in the sandbox (repo + /opt/neewa/status mounted read-only). Return a compact parsable
summary. Do not write repo files, add listeners, spend, or send external messages.

## Requested checks

1. Host truth from `/opt/neewa/status/latest.json`: cite generated_at, staleness, host,
   gateway/docker/tailscaled/ollama, repo head/state, jobs. Confirm it is the host, not the container.
2. Voice truth from `/opt/neewa/status/voice.json`: cite generated_at, overall, and last observed
   wake/STT/turn events.
3. Confirm the 4 scheduled jobs exist (daily-health, morning-brief, host-status, voice-readiness)
   and that wake_word.start_new_session is now false.
4. Independent review of this delivery's committed changes (Cursor will give the commit SHA):
   verify no secrets in repo/plugin JS, read-only mounts intact, no public listener, the NEEWA Home
   plugin uses only supported APIs, and voice task delegation still honors approval gates.

## Required response (parsable)

`job_id`, `source_commit`, `host_snapshot_timestamp`, `voice_snapshot_timestamp`, `observed_state`,
`defects`, `actions_performed`, `actions_not_performed`, `approval_required`, `final_status`
(DONE | BLOCKED | APPROVAL_REQUIRED). Never claim a Windows physical/UI/audio PASS from the headless host.
