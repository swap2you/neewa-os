# Step 4 — Isolated project portfolio registry

Canonical index: `11_CONFIG/projects.json` (extended, not duplicated).
Nine inventoried personal trees each have one registry record and one
`evidence/PROJECT_PORTFOLIO/<id>/` context. Denied and unclassified folders
are listed in `skipped`. No raw workspace trees or secrets were committed.
