# Step 5 — Registry deployment

Pushed `9a1bd3b` to `origin/main` (fast-forward, no force).
`ubuntu@neewa-core-01:/opt/neewa/neewa-os` pulled `--ff-only` to the same
commit. `11_CONFIG/projects.json` and `evidence/PROJECT_PORTFOLIO/` are
present. No source edits, no denied/unclassified access, no deletions.
